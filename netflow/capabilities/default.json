{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Capabilities granted to the main window. Only core APIs are required because all data flows through invoke commands defined in main.rs.",
  "windows": ["main"],
  "permissions": ["core:default", "dialog:default"]
}
